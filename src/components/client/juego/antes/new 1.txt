instalar máquina virtual ubuntu


ir al directorio raíz de la práctica

sudo docker-compose -f docker/config/base.yml build...
interesan auth server y hub
docker ps -a (saldrán los servicios creados)	


casca y no se sabe
sudo docker container logs dms1920-auth-server (nombre del anterior/ la salida que da es el .py y escucha en el 1234)

sudo docker exec -it dms1920-auth-server /bin/bash (saca terminal dentro del contenedor)
		ps auxf --> muestra el contenido
		

curl http://172.10.1.10:1234/ (si pone running es que está yendo) -v(para ver todo)


sudo docker exec -it dms1920-auth-server /bin/bash
	-f docker/config/base.yml up -d

-------------------------SIN DOCKER----------------------------
cd src/components/auth-server
AUTH_SERVER_DATABASE_PATH=/tmp/data/database.db bin/rest-server.sh





hacer un script para levantar todo








